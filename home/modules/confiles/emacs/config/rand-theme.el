;; En config/rand-theme.el
(use-package rand-theme
  :config
  (setq rand-theme-wanted '(doom-challenger-deep 
                            doom-outrun-electric 
                            doom-vibrant 
                            doom-dracula))
  (rand-theme))
